{
  "name": "wedding-invitation-studio",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check editor.js && node --check login.js && node --check dashboard.js && node --check responses.js"
  },
  "engines": {
    "node": ">=20"
  }
}
